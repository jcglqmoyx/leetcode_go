package main

/*
func maxSubArray(nums []int) int {
	sum, res := 0, math.MinInt32
	max := func(a, b int) int {
		if a > b {
			return a
		}
		return b
	}
	for _, num := range nums {
		sum = max(sum+num, num)
		res = max(res, sum)
	}
	return res
}
*/
